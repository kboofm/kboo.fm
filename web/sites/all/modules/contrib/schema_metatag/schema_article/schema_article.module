<?php

/**
 * @file
 * Metatag integration for the schema_article module.
 */

/**
 * Implements hook_ctools_plugin_api().
 */
function schema_article_ctools_plugin_api($owner, $api) {
  if ($owner == 'metatag' && $api == 'metatag') {
    return array('version' => 1);
  }
}
