<?php

/**
 * @file
 * Metatag integration for the schema_video_object module.
 */

/**
 * Implements hook_ctools_plugin_api().
 */
function schema_video_object_ctools_plugin_api($owner, $api) {
  if ($owner == 'metatag' && $api == 'metatag') {
    return array('version' => 1);
  }
}
