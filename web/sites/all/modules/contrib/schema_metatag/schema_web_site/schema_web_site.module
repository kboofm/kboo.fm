<?php

/**
 * @file
 * Metatag integration for the schema_web_site module.
 */

/**
 * Implements hook_ctools_plugin_api().
 */
function schema_web_site_ctools_plugin_api($owner, $api) {
  if ($owner == 'metatag' && $api == 'metatag') {
    return array('version' => 1);
  }
}
