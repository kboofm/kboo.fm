<?php

/**
* Implements hook_node_presave()
*
* @param $node
*/
function radio_person_profile_node_presave($node) {
  if ($node->type != 'person_profile') {
    return;
  }

  if (empty($node->title)) {
    $node->title = PersonProfileContentType::getFullName($node);
  }
}
