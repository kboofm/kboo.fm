<?php

/**
 * Implements hook_cron().
 */
function radio_station_cron() {
  # query the next 5 shows
  $station = new RadioStation();
  $station->createNextEpisodes(5);

  # create episodes if needed

  # schedule ripping if show starts in less 1 hour

  # after ripping, attach media to episode

  # delete any older media as needed
}